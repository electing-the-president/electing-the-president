{{#em-form model=credentials action="authenticate"}}
    {{em-input property="identity" label="Email" placeholder="Enter your email..."}}
    {{em-input property="password" label="Password" placeholder="And password..." type="password" disabled=nameHasValue}}
{{/em-form}}
